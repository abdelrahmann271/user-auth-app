# MongoDB Connection (with auth)
# For local dev with Docker: mongodb://easygen:easygen_dev_password@localhost:27017/easy-gen-app?authSource=admin
MONGODB_URI=mongodb://username:password@localhost:27017/easy-gen-app?authSource=admin

# JWT Configuration
# Generate with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1800

# Application
PORT=3001
NODE_ENV=development

# Production settings (optional)
# FRONTEND_URL=https://yourdomain.com
# ENABLE_SWAGGER=false
